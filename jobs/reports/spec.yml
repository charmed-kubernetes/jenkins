plan:
  - script:
      - python3 jobs/reports/generate-reports.py build

meta:
  mkdocs:
    destination:
      - "reporters/index.md"
  name: Reports
  description: |
    Generates reports from build statuses
