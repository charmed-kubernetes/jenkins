meta:
  name: Verify CK
  synopsis:
    - summary: Running the base validation suite against a deployed Kubernetes
      code: |
        ```
        # edit spec.yml and update the appropriate vars under the `env:` section
        > ogc --spec jobs/validate/spec.yml -t core
        ```
  description: |
    Verifies that CK passes integration tests
  mkdocs:
    destination:
      - "validations/ck/index.md"

matrix:
  snap_version:
    - 1.18/edge
    - 1.17/edge
    - 1.16/edge
    - 1.15/edge
  series:
    - focal
    - bionic
    - xenial
  channel:
    - edge
  arch:
    - amd64

concurrent: no

plan:
  env:
    - JUJU_DEPLOY_BUNDLE=cs:~containers/charmed-kubernetes
    - JUJU_DEPLOY_CHANNEL=$CHANNEL
    - JUJU_CLOUD=aws/us-east-1
    - JUJU_CONTROLLER=validate-ck
    - JUJU_MODEL=validate-ck

  post-execute: |
    #!/bin/bash
    set -x
    export JUJU_CONTROLLER=$(ogc-collect get-key juju_controller)
    export JUJU_MODEL=$(ogc-collect get-key juju_model)

    wget https://raw.githubusercontent.com/juju-solutions/cdk-field-agent/master/collect.py
    python collect.py -m $JUJU_CONTROLLER:$JUJU_MODEL

    ogc-collect set-key 'snap_version' "$SNAP_VERSION"
    ogc-collect set-key 'juju_deploy_channel' "$JUJU_DEPLOY_CHANNEL"

    tar cvjf artifacts.tar.gz * || [[ $? -eq 1 ]]
    ogc-collect push 'artifacts' artifacts.tar.gz
    juju destroy-controller -y --destroy-all-models --destroy-storage $JUJU_CONTROLLER

  execute: |
    #!/bin/bash
    set -eu

    export JUJU_CONTROLLER="$JUJU_CONTROLLER-$(ogc-collect get-key job_id | cut -f1 -d-)"
    export JUJU_MODEL="$JUJU_MODEL-$SERIES-$(ogc-collect get-key job_id | cut -f1 -d-)"

    ogc-collect set-key 'juju_controller' "$JUJU_CONTROLLER"
    ogc-collect set-key 'juju_model' "$JUJU_MODEL"
    ogc-collect set-key 'job_name_custom' "validate-ck-$SERIES-$SNAP_VERSION"

    juju bootstrap $JUJU_CLOUD $JUJU_CONTROLLER \
         -d $JUJU_MODEL \
         --bootstrap-series $SERIES \
         --force \
         --bootstrap-constraints arch=$ARCH \
         --model-default test-mode=true \
         --model-default resource-tags=owner=k8sci \
         --model-default image-stream=daily  1>&2

    tee overlay.yaml <<EOF> /dev/null
    series: $SERIES
    applications:
      kubernetes-master:
        options:
          channel: $SNAP_VERSION
      kubernetes-worker:
        options:
          channel: $SNAP_VERSION
    EOF

    juju deploy -m $JUJU_CONTROLLER:$JUJU_MODEL \
          --overlay overlay.yaml \
          --force \
          --channel $JUJU_DEPLOY_CHANNEL $JUJU_DEPLOY_BUNDLE 1>&2

    juju-wait -e $JUJU_CONTROLLER:$JUJU_MODEL -w

    timeout 45m pytest -m "not slow" $WORKSPACE/jobs/integration/validation.py \
       --cloud $JUJU_CLOUD \
       --model $JUJU_MODEL \
       --controller $JUJU_CONTROLLER
